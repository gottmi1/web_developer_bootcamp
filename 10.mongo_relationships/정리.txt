one to few 관계

1 : 다수에서 사용자가 가지는 값이 많지 않을 때 쓰인다. (저장해야 하는 정보의 양이 적을 때)
부모 안에 직접 정보를 넣는다.

one to many 관계

데이터의 양이 그렇게 크지 않을 때 사용할 수 있다.
부모 안에 직접 정보를 넣지않고 populate method로 받아온다.

one to bajillions 

한 문서에 연결되어있는 자식 문서나 정보들이 많을 때 사용하는 방법이다. 트위터나 인스타그램등
많은 양에 데이터가 있을 때는 자식에 대한 레퍼런스를 부모 문서에 저장하는 것이 다른 경우보다 효과적이기 때문에 사용된다.

populate

우리가 볼 순 없지만 복잡한 쿼리를 만들어 mongo에게 전달하한다, 그럼 mongo는 관련된 정보를 모두 가져온다.
정보를 받는 데에 유용하게 쓰인다.
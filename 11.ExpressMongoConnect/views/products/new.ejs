<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>New Product</title>
</head>
<body>
  <h1><%=farm.name%>에 상품 추가하기</h1>
  <form action="/farms/<%=farm._id%>/products" method="POST"> 
    <!-- 경로 첫 부분에 슬래시를 안 해주면 링크가 초기화되지 않고 계속 중첩되기 때문에 꼭 해줘야 함. -->
    <label for="product">Product Name</label>
    <input type="text" id="product" name="name" placeholder="product name"> <br />
    <label for="price">Product Price</label> 
    <input type="number" id="price" name="price" placeholder="pridce"> <br />
    <label for="category">category</label>
    <select name="category" id="category">
      <% for(let category of categories) {%>
        <option value="<%=category%>"><%=category%></option>
      <% } %>
    </select>
    <button>Add</button>
  </form>
  <a href="/farms/<%=farm._id%>">돌아가기</a>
</body>
</html>
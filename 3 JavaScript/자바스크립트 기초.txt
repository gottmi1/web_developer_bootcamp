넘버

NaN = 숫자로 인식되지만 숫자가 아닌 값
Not a Number 지만 typeof로 보면 number임

문자열

메서드 뒤에 ()를 붙이지 않으면 실제로는 아무 작업도 하지않음.

.trim (문자열 앞이나 뒤에 공백을 없애줌)

많은 메소드는 괄호안에 argument 값을 받음
아규먼트는 동작 방식을 변경하기 위해 메소드에 전달할 수 있는 입력일 뿐이다.

slice

let asda = str.slice(시작할 index, 거기부터 출력하고 싶은 글자 수 index아님);
str 문자열에서 시작할 인덱스부터 끝낼 인덱스까지 잘라서 새 문자열을 만듬
알규먼트를 하나만 입력하면 앞에서 그만큼 띄우고 함. -를 입력하면뒤부터 ex -1은 마지막 글자만 출력

replace

("asdf", "hhhh") 앞 알규먼트를 뒤 알규먼트로 대체시킴.

문자열 템플릿

``겠지

언디파인드와 널

언디파인드 = 정이되지 않음 js: 모르겠어요
null = 의도적으로 값이 없게 함 얘는 거의 안씀

Math

around : 평범한 반올림
floor : 그냥 뒤에있는 소수점을 지워버림 = 무조건 내림
ceil : 2.0000000..1 -> 3 으로 바꿈 = 무조건 올림
random : 0.0000000000...1 ~ 0.9999999... 
pow : 앞숫자 ** 뒷숫자 거듭제곱시킴



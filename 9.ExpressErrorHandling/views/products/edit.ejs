<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit Product</title>
</head>

<body>
  <h1>Edit product</h1>
  <form action="/products/<%=product._id%>?_method=PUT" method="POST">
    <label for="product">Product Name</label>
    <input type="text" id="product" name="name" placeholder="product name" value="<%=product.name%>"> <br />
    <label for="price">Product Price</label>
    <input type="number" id="price" name="price" placeholder="pridce" value="<%=product.price%>"> <br />
    <label for="category">category</label>
    <select name="category" id="category">
      <!-- 이렇게 루프를 설정해 놓으면 index.js에서 하드코딩한 categories 배열에 다른 것이 들어와도 해당 요소를 자동으로 생성해주기 때문에 편리하다.유효성 검사중이라면 enum에도 추가해줘야 함) -->
      <% for(let category of categories) {%>
        <option value="<%=category%>" <%=product.category === category ? 'selected' : ''%>><%=category%></option>
      <% } %>
    </select>
    <button>Add</button>
  </form>

  <a href="/products/<%=product._id%>">Cancel</a>
</body>

</html>